# Database Configuration
# Для розробки (SQLite)
DATABASE_URL=sqlite:///./contacts_dev.db

# Для production (PostgreSQL) - розкоментуйте та налаштуйте
# DATABASE_URL=postgresql://postgres:password@localhost:5432/contacts_db

# Security
# ЗМІНІТЬ ЦЕ В PRODUCTION!
SECRET_KEY=your-super-secret-key-change-this-in-production

# Додаткові налаштування
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# Email verification settings
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
FROM_EMAIL=your-email@gmail.com

# Rate limiting settings
RATE_LIMIT_PER_MINUTE=10
RATE_LIMIT_PER_HOUR=100

# Cloudinary settings (для завантаження аватарів)
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# CORS settings
CORS_ORIGINS=["http://localhost:3000", "http://localhost:8080", "https://yourdomain.com"] 